version: '3.8'

services:
  py-editor:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./workspace:/app/workspace
      - ./snippets:/app/snippets
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: python-ide